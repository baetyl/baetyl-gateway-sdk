if(NOT PROTO_DIR)
    message(SEND_ERROR "PROTO_DIR not set")
endif()

set(DRIVER_PROTO_ROOT_DIR "${PROTO_DIR}/generated/install-${SUFFIX}")
set(DRIVERPROTO_ROOT_DIR "${DRIVER_PROTO_ROOT_DIR}")
set(DRIVER_PROTO_INCLUDE_DIR "${DRIVER_PROTO_ROOT_DIR}/include")
set(DRIVERPROTO_INCLUDE_DIR "${DRIVER_PROTO_INCLUDE_DIR}")
set(DRIVER_PROTO_LIB_DIR "${DRIVER_PROTO_ROOT_DIR}/lib")
set(DRIVERPROTO_LIB_DIR "${DRIVER_PROTO_LIB_DIR}")
set(DRIVER_PROTO_INCLUDE_DIRS "${DRIVER_PROTO_INCLUDE_DIR}")
set(DRIVERPROTO_INCLUDE_DIRS "${DRIVER_PROTO_INCLUDE_DIRS}")
set(DRIVER_PROTO_INCLUDEDIR "${DRIVER_PROTO_INCLUDE_DIRS}")
set(DRIVERPROTO_INCLUDEDIR "${DRIVER_PROTO_INCLUDEDIR}")
set(DRIVER_PROTO_LIBRARY "${DRIVER_PROTO_ROOT_DIR}/lib")
set(DRIVERPROTO_LIBRARY "${DRIVER_PROTO_LIBRARY}")
set(DRIVER_PROTO_LIBRARY_DIRS "${DRIVER_PROTO_LIBRARY}")
set(DRIVERPROTO_LIBRARY_DIRS "${DRIVER_PROTO_LIBRARY}")

file(GLOB DRIVER_PROTO_LIST "${DRIVER_PROTO_LIBRARY}/*.a")
file(GLOB DRIVERPROTO_LIST "${DRIVER_PROTO_LIBRARY}/*.a")
set(DRIVER_PROTO_LIBRARIES ${DRIVER_PROTO_LIST})
set(DRIVERPROTO_LIBRARIES ${DRIVER_PROTO_LIST})
