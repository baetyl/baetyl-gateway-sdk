if(NOT THIRDPART_DIR)
    message(SEND_ERROR "THIRDPART_DIR not set")
endif()

set(JSONCPP_ROOT_DIR "${THIRDPART_DIR}/jsoncpp/install-${SUFFIX}")
set(JSONCPP_INCLUDE_DIR "${JSONCPP_ROOT_DIR}/include")
set(JSONCPP_LIB_DIR "${JSONCPP_ROOT_DIR}/lib")
set(JSONCPP_INCLUDE_DIRS "${JSONCPP_INCLUDE_DIR}")
set(JSONCPP_INCLUDEDIR "${JSONCPP_INCLUDE_DIRS}")
set(JSONCPP_LIBRARY "${JSONCPP_ROOT_DIR}/lib")
set(JSONCPP_LIBRARY_DIRS "${JSONCPP_LIBRARY}")

file(GLOB JSONCPP_LIST "${JSONCPP_LIBRARY}/*.a")
set(JSONCPP_LIBRARIES ${JSONCPP_LIST})

message("-- JSONCPP_ROOT_DIR: ${JSONCPP_ROOT_DIR}")
